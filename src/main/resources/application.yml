server:
  port: 80
  servlet:
    context-path: /

spring:
   datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://localhost:3306/db_jxc2
      username: root
      password: root
   jpa:
     hibernate.ddl-auto: update
     show-sql: true
   thymeleaf:
    cache: false